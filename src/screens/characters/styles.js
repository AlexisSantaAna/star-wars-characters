import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    fontSize: 20,
  },
  container: {
    margin: 10,
    borderRadius: 6,
    borderColor: "#fff",
    borderWidth: 2,
  },
  externalContainer: {
    flex: 1,
    backgroundColor: "#000"
  }
});
