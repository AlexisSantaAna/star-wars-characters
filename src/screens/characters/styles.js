import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    fontSize: 20,
  },
  container: {
    margin: 20,
    borderRadius: 6,
    borderColor: "#fff",
    borderWidth: 2
  },
  externalContainer: {
    flex: 1,
    backgroundColor: "#000"
  },
  loader: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center"
  }
});
